/**
 * Stellar config — re-exports from lib/constants so there is
 * exactly one place to change network settings.
 */
export {
  HORIZON_URL,
  NETWORK_PASSPHRASE,
  FRIENDBOT_URL,
  USDC_CODE,
  USDC_ISSUER,
  BASE_FEE,
} from "@/lib/constants";
